// Import necessary modules
import { extendTheme } from "@chakra-ui/react";

// Define custom theme
const customTheme = extendTheme({
  fonts: {
    body: "Roboto, sans-serif", // Change "Roboto" to your desired font family
    heading: "Roboto, sans-serif", // Change "Roboto" to your desired font family
  },
});

export default customTheme;
